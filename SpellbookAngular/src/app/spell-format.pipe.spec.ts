import { SpellFormatPipe } from './spell-format.pipe';

describe('SpellFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SpellFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
